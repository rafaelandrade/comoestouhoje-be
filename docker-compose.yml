version: '3'

services:
    web:
        env_file: .env
        build: .
        restart: always
        container_name: comoestouhoje-be
        ports: ['5000:5000']
        volumes:
            - .:/app
        command: bash -c 'uvicorn app.main:app --host 0.0.0.0 --reload --port 5000'
